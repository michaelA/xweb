<%= form_for(@model_image) do |f| %>
  <% if @model_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@model_image.errors.count, "error") %> prohibited this model_image from being saved:</h2>

      <ul>
      <% @model_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_url %><br />
    <%= f.text_field :image_url %>
  </div>
  <div class="field">
    <%= f.label :gender %><br />
    <%= f.text_field :gender %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
